"use client";

import styles from "@/styles/Footer.module.css";

export const Footer = () => {
    return (
        <div className={styles.container}>
            <p className={styles.text}>Dibangun dengan Next.js:<br/>mustakuusi — lahir dari pikiran yang tak diam.</p>
        </div>
    );
};